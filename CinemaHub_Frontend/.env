VITE_API_URL=http://localhost:18080
# Optional: booking service exposed on host (docker 18083) if gateway returns 404 on /confirm until you rebuild api-gateway
# VITE_BOOKING_URL=http://localhost:18083
